import { STORAGE_PREFIX } from '../constants';

export const SCHEMA_VERSION_STORAGE_KEY = `${STORAGE_PREFIX}__schema_version__`;
export const CONFIG_STORAGE_KEY = `${STORAGE_PREFIX}config`;
export const LAST_OUTPUT_STORAGE_KEY = `${STORAGE_PREFIX}last-output`;
export const LAST_OUTPUT_UPDATED_AT_KEY = `${STORAGE_PREFIX}last-output-updated-at`;
export const WISH_OFFS_STORAGE_KEY = `${STORAGE_PREFIX}wishoffs:2025-12`;
